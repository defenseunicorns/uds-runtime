kind: ZarfPackageConfig
metadata:
  name: uds-runtime
  description: tbd
  version: 0.1.0

components:
  - name: uds-runtime
    required: true
    images:
      - ghcr.io/unclegedd/uds-runtime-dev:dev
    charts:
      - name: uds-runtime
        localPath: ./chart
        namespace: uds-runtime
        version: 0.1.0
