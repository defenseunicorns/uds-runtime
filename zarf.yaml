kind: ZarfPackageConfig
metadata:
  name: uds-runtime
  description: tbd
  version: '###ZARF_PKG_TMPL_PKG_VERSION###'

components:
  - name: uds-runtime
    required: true
    images:
      - ghcr.io/defenseunicorns/uds-runtime:###ZARF_PKG_TMPL_IMAGE_TAG###
    charts:
      - name: uds-runtime
        localPath: ./chart
        namespace: uds-runtime
        version: 0.1.0
